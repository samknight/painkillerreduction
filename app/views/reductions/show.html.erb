<h1>New Reduction Plan for <%= "Patient #{@medication.patient.id}" %></h1>
<p>Drug: <%= @medication.drug.full_name %>
<br />Dose: <%= @medication.dose %>
<br />Frequency: <%= @medication.frequency %> times a day
<br /><strong>Morphine Equivalent:</strong> <%= @medication.morphine_equivalent %>mg</p>


<div class="nhsuk-table-responsive">
  <table class="nhsuk-table">
    <caption class="nhsuk-table__caption">Morphone Reduction Plan</caption>
    <thead class="nhsuk-table__head">
      <tr class="nhsuk-table__row">
        <th class="nhsuk-table__header" scope="col">Week / Wythnos</th>
        <% Interval.times[@medication.frequency].each do |time| %>
            <th class="nhsuk-table__header" scope="col"><%= time %></th>
        <% end %>
      </tr>
    </thead>
    <tbody class="nhsuk-table__body">
      <% (@reduction.days / 7).times do |i| %>
        <tr class="nhsuk-table__row">
            <td class="nhsuk-table__cell"><%= "Week #{i + 1}" %></td>
            <% @reduction.schedule(i).each_with_index do |schedule, j| %>
                <td class="nhsuk-table__cell "><%= schedule %></td>
            <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
